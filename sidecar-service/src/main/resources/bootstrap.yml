## YAML Template.
spring:
  profiles:
    active: local
  application:
    name: gateway
eureka:
  instance:
    prefer-ip-address: true
   
---
spring:
  profiles: docker
  application:
    name: gateway
  cloud:
    config:
      uri: http://configserver:8888
encrypt:
  failOnError: false
