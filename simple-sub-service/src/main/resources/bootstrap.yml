## YAML Template.
eureka:
  client:
    serviceUrl:
#      defaultZone: ${vcap.services.eureka-service.credentials.uri:http://127.0.0.1:8761}/eureka/
        defaultZone: http://eureka:8761/eureka/
      
spring:
  application:
    name: simple-sub-service
